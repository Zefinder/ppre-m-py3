package pokemon.event;

/**
 * Interface that represents an event. Events are managed by the EventManager
 * (and not the event itself). See {@link EventManager} for more information
 */
public interface Event {

}
